- title((_("Creating %s") % current_controller_gettext_human_name))
= error_messages_for :status 
.standard_box
  .hako
    - form_for @status, :url => statuses_path, :html => { :method => :post, :class => "hakozaki" } do |form|
      = render :partial => "common/actions/for_form_new"  
      %h1= yield(:title)
      %hr/
      = render :partial => "form", :locals => {:form => form}
      = render :partial => "common/actions/for_form_new"   