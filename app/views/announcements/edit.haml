- title((_("Editing %s") % _("Announcement")))    
= error_messages_for :announcement
.standard_box
  .hako
    - form_for(@announcement, :html => { :class => "hakozaki" }) do |form|
      = render :partial => "common/actions/for_form_edit"
      %h1= yield(:title)
      %hr/
      = render :partial => "form", :locals => {:form => form}
      = render :partial => "common/actions/for_form_edit"   
