- title((I18n.t("Editing {{value}}", :default => "Editing {{value}}", :value => current_controller_gettext_human_name)))
= error_messages_for :name_prefix  
.standard_box
  .hako
    - form_for @name_prefix, :url => name_prefix_path, :html => { :method => :put, :class => "hakozaki", :autocomplete => :off } do |form|     
      = render :partial => "common/actions/for_form_edit"
      %h1= yield(:title)
      %hr/
      = render :partial => "form", :locals => {:form => form}
      = render :partial => "common/actions/for_form_edit"
