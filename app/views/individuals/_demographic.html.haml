%h2= I18n.t("Demographic Information", :default => "Demographic Information")
%label{:for => "contact_gender_id", :class => "w25 inputselect"}
  %span.wrapper
    = label_with_lookup :controller => :gender
    = text_field_with_auto_complete :presenter, :contact_gender_name, { :class => "field" }, { :url => formatted_lookup_genders_path(:js), :method => :get, :skip_style => true, :with => "'search=' + element.value" }
%label{:for => "contact_date_of_birth", :class => "w25 inputselect"}
  %span.wrapper
    %span.title= I18n.t("Date of Birth", :default => "Date of Birth")
    = form.calendar_date_select :contact_date_of_birth, :popup => :force , :valid_date_check => "date < (new Date()).stripTime()", :class => "field"    
%label{:for => "contact_marital_status_id", :class => "w25 inputselect"}
  %span.wrapper
    = label_with_lookup :controller => :marital_status
    = text_field_with_auto_complete :presenter, :contact_marital_status_name, { :class => "field" }, { :url => formatted_lookup_marital_statuses_path(:js), :method => :get, :skip_style => true, :with => "'search=' + element.value" }
%label{:for => "contact_ethnicity_id", :class => "w25 inputselect"}
  %span.wrapper
    = label_with_lookup :controller => :ethnicity
    = text_field_with_auto_complete :presenter, :contact_ethnicity_name, { :class => "field" }, { :url => formatted_lookup_ethnicities_path(:js), :method => :get, :skip_style => true, :with => "'search=' + element.value" } 
.clear

%label{:for => "contact_country_of_birth_id", :class => "w25 inputselect"}
  %span.wrapper
    %span.title= I18n.t("Country of Birth", :default => "Country of Birth")
    = text_field_with_auto_complete :presenter, :contact_country_of_birth_name, { :class => "field" }, { :url => formatted_lookup_countries_path(:js), :method => :get, :skip_style => true, :with => "'search=' + element.value" }
%label{:for => "contact_citizenship_id", :class => "w25 inputselect"}
  %span.wrapper
    %span.title= I18n.t("Citizenship", :default => "Citizenship")
    = text_field_with_auto_complete :presenter, :contact_citizenship_name, { :class => "field" }, { :url => formatted_lookup_countries_path(:js), :method => :get, :skip_style => true, :with => "'search=' + element.value" }
%label{:for => "contact_nationality_id", :class => "w25 inputselect"}
  %span.wrapper
    %span.title= I18n.t("Nationality", :default => "Nationality")
    = text_field_with_auto_complete :presenter, :contact_nationality_name, { :class => "field" }, { :url => formatted_lookup_countries_path(:js), :method => :get, :skip_style => true, :with => "'search=' + element.value" }
%label{:for => "contact_is_foreign", :class => "w25 radiocheck"}
  %span.wrapper
    %br/
    = form.check_box :contact_is_foreign, :class => "select"
    %span.title= I18n.t("Is Foreign", :default => "Is Foreign")
.clear

%label{:for => "contact_is_deceased", :class => "w25 radiocheck"}
  %span.wrapper
    %br/
    = form.check_box :contact_is_deceased, :class => "select"
    %span.title= I18n.t("Is Deceased", :default => "Is Deceased")   
%label{:for => "contact_deceased_date", :class => "w25 inputselect"}
  %span.wrapper
    %span.title= I18n.t("Deceased Date", :default => "Deceased Date")
    = form.calendar_date_select :contact_deceased_date, :popup => :force, :class => "field"
.clear

