- title((I18n.t("Creating {{value}}", :default => "Creating {{value}}", :value => I18n.t("Individual", :default => "Individual"))))
= error_messages_for :presenter 
.individual_box 
  .hako
    - form_for :presenter, :url => individuals_path, :html => { :method => :post, :class => "hakozaki" } do |form|
      = render :partial => "contacts/actions/for_form_new"  
      %h1= yield(:title)
      %hr/
      = render :partial => "form", :locals => {:form => form}
      = render :partial => "contacts/actions/for_form_new"