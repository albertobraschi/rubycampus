- title((_("Editing %s") % current_controller_gettext_human_name))
= error_messages_for :academic_level  
.standard_box
  .hako
    - form_for @academic_level, :url => academic_level_path, :html => { :method => :put, :class => "hakozaki" } do |form|     
      = render :partial => "common/actions/for_form_edit"
      %h1= yield(:title)
      %hr/
      = render :partial => "form", :locals => {:form => form}
      = render :partial => "common/actions/for_form_edit"
