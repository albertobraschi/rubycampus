- if @total == 0
  .no_data{:style=>"text-align:center;"}= _("No %s Found") % current_controller_gettext_human_name_pluralized
- else
  %table
    %tr
      %th{ :class => (sort_th_class_helper 'name') }
        = sort_link_helper _("Name"), 'name'
      %th{ :class => (sort_th_class_helper 'is_default') }
        = sort_link_helper _("Default"), 'is_default'
      %th{ :class => (sort_th_class_helper 'is_enabled') }
        = sort_link_helper _("Enabled"), 'is_enabled'
      %th
    - @academic_levels.each do |academic_level|
      %tr{:class => cycle('even','odd'), :style => "#{'color: #f80a12;' unless academic_level.is_enabled}"}
        %td=h academic_level.name
        %td=h academic_level.is_default ? _('Yes') : _('No')
        %td=h academic_level.is_enabled ? _('Yes') : _('No')
        %td= render :partial => "common/actions/for_row_index_toggle_lookup", :locals => { :local => academic_level }
  - if @academic_levels_pages.page_count > 1
    %div.pagination
      = pagination_links_remote @academic_levels_pages