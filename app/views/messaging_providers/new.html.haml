- title((I18n.t("Creating {{value}}", :default => "Creating {{value}}", :value => current_controller_gettext_human_name)))
= error_messages_for :messaging_provider 
.standard_box
  .hako
    - form_for @messaging_provider, :url => messaging_providers_path, :html => { :method => :post, :class => "hakozaki" } do |form|
      = render :partial => "common/actions/for_form_new"  
      %h1= yield(:title)
      %hr/
      = render :partial => "form", :locals => {:form => form}
      = render :partial => "common/actions/for_form_new"   