- if @total == 0
  .no_data{:style=>"text-align:center;"}= _("No %s Found") % current_controller_gettext_human_name_pluralized
- else
  %table
    %tr
      %th{ :class => (sort_th_class_helper 'name') }
        = sort_link_helper _("Name"), 'name'
      %th{ :class => (sort_th_class_helper 'is_default') }
        = sort_link_helper _("Default"), 'is_default'
      %th{ :class => (sort_th_class_helper 'is_enabled') }
        = sort_link_helper _("Enabled"), 'is_enabled'
      %th
    - @messaging_providers.each do |messaging_provider|
      %tr{:class => cycle('even','odd'), :style => "#{'color: #f80a12;' unless messaging_provider.is_enabled}"}
        %td=h messaging_provider.name
        %td=h messaging_provider.is_default ? _('Yes') : _('No')
        %td=h messaging_provider.is_enabled ? _('Yes') : _('No')
        %td= render :partial => "common/actions/for_row_index_toggle_lookup", :locals => { :local => messaging_provider }
  - if @messaging_providers_pages.page_count > 1
    %div.pagination
      = pagination_links_remote @messaging_providers_pages